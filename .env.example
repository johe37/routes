# Optional. Improves loop quality (round-trip). Without it, street routing
# still works via OpenStreetMap (FOSSGIS OSRM).
# Get one at https://openrouteservice.org (account.heigit.org).
ORS_API_KEY=

# Optional MapLibre style override. Default is OpenFreeMap Liberty.
# NEXT_PUBLIC_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/liberty
